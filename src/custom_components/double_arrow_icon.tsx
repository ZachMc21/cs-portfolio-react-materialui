import { styled } from "@mui/material/styles";
import DoubleArrowIcon from "@mui/icons-material/DoubleArrow";

const StyledDoubleArrowIcon = styled(DoubleArrowIcon)(({ }) => ({
    size: "medium"
}));

export default StyledDoubleArrowIcon;